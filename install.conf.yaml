- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link: 
    # ~/.bashrc: bashrc.bash
    ~/.zshrc: zshrc
    ~/.zlogin: zlogin
    ~/.zimrc: zimrc
    ~/.zim/modules/prompt/functions/prompt_best_setup: best.zsh-theme
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf

- shell: []
