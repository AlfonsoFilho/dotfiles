- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link: 
    ~/.zshrc: zshrc
    ~/.zlogin: zlogin
    ~/.gitconfig: gitconfig
    ~/.p10k.zsh: p10k.zsh
    # ~/.tmux.conf: tmux.conf
    
- shell: []
